// 3. Destructuring Assignment:


// Обычное присваивание
const person = { first_name: 'Bob', age: 30 };
const first_name = person.first_name;
const age = person.age;

// Синтаксический сахар с деструктуризацией
// const { first_name, age } = person;

// { $route, $axios } = this;

// const { data } = response